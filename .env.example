PORT=3001

# Get this from Atlas: Database -> UrlShortener07032026 -> Connect -> Drivers
# The hostname is NOT "UrlShortener07032026" — it looks like:
# urlshortener07032026.ab12cd.mongodb.net  (random suffix assigned by Atlas)
MONGODB_URI=mongodb+srv://brackesb12_db_user:YOUR_PASSWORD@PASTE-HOST-FROM-ATLAS.mongodb.net/urlShortener?retryWrites=true&w=majority
